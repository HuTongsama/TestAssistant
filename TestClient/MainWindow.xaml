<Window x:Class="TestClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i ="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:TestClient"
        mc:Ignorable="d"
        Title="MainWindow" Height="488" Width="873">
    <Window.Resources>
        <Style x:Key="ListItemStyle" TargetType="ListBoxItem">
            <Style.Setters>
                <Setter Property="Content" Value="{Binding ItemName}"></Setter>
                <Setter Property="IsSelected" Value="{Binding IsSelected}"></Setter>
            </Style.Setters>
            <Style.Triggers>
                <DataTrigger Binding="{Binding UseCustomerRightClick}" Value="true">
                    <Setter Property="local:ListItemAttachedBehaviour.MouseRightClickCommand" Value="{Binding MouseRightClickCommand}"></Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <ControlTemplate x:Key="TabGridTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="260*"/>
                    <ColumnDefinition Width="120*"/>
                    <ColumnDefinition Width="250*"/>
                </Grid.ColumnDefinitions>
                <Grid HorizontalAlignment="Center" Height="256" Margin="0,10,0,10" VerticalAlignment="Top" Width="258" Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="5*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <ListView ItemsSource="{Binding KeyToCheckState}" Grid.Column="1">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding StateKey}" IsChecked="{Binding State}"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <ListBox ItemsSource="{Binding PictureSetList}" ItemContainerStyle="{StaticResource ListItemStyle}"
                                             SelectionMode="Multiple"/>
                    
                </Grid>
                <ListBox ItemsSource="{Binding SelectedPicList}" ItemContainerStyle="{StaticResource ListItemStyle}" SelectionMode="Multiple"
                                         HorizontalAlignment="Center" Height="256" Margin="0,10,0,10" VerticalAlignment="Top" Width="120" Grid.Column="1">
                </ListBox>
                <Grid Height="256" Width="250" Margin="0,10,0,10" HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Column="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"></ColumnDefinition>
                        <ColumnDefinition Width="5*"></ColumnDefinition>
                        <ColumnDefinition Width="1*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"></RowDefinition>
                        <RowDefinition Height="1*"></RowDefinition>
                        <RowDefinition Height="1*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Label Content="默认模板" Margin="0,30,0,30" Grid.Row="0" Grid.Column="0"></Label>
                    <TextBox Text="{Binding DefaultTemplate}" Margin="0,30,0,30" Grid.Row="0" Grid.Column="1"></TextBox>
                    <Button Content=".." Command="{Binding DefaultTemplateTemplateButtonCommand}" Margin="0,30,0,30" Grid.Row="0" Grid.Column="2"></Button>

                    <Label Content="x86DllPath" Margin="0,30,0,30" Grid.Row="1" Grid.Column="0"></Label>
                    <TextBox Text="{Binding X86Path}" Margin="0,30,0,30" Grid.Row="1" Grid.Column="1"></TextBox>
                    <Button Content=".." Command="{Binding X86PathButtonCommand}" Margin="0,30,0,30" Grid.Row="1" Grid.Column="2"></Button>

                    <Label Content="x64DllPath" Margin="0,30,0,30" Grid.Row="2" Grid.Column="0"></Label>
                    <TextBox Text="{Binding X64Path}" Margin="0,30,0,30" Grid.Row="2" Grid.Column="1"></TextBox>
                    <Button Content=".." Command="{Binding X64PathButtonCommand}" Margin="0,30,0,30" Grid.Row="2" Grid.Column="2"></Button>
                </Grid>
            </Grid>
        </ControlTemplate>
        <Style x:Key="TabItemStyle" TargetType="TabItem">
            <Setter Property="Header" Value="{Binding Header}"></Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Header}" Value="DBR">
                    <Setter Property="Content">
                        <Setter.Value>
                            <ContentControl Template="{StaticResource TabGridTemplate}"></ContentControl>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
                <DataTrigger Binding="{Binding Header}" Value="DLR">
                    <Setter Property="Content">
                        <Setter.Value>
                            <ContentControl Template="{StaticResource TabGridTemplate}"></ContentControl>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
                <DataTrigger Binding="{Binding Header}" Value="DCN">
                    <Setter Property="Content">
                        <Setter.Value>
                            <ContentControl Template="{StaticResource TabGridTemplate}"></ContentControl>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="383*"/>
            <ColumnDefinition Width="409*"/>
        </Grid.ColumnDefinitions>
        <TabControl ItemsSource="{Binding TabItems.Values}" ItemContainerStyle="{StaticResource TabItemStyle}" SelectedItem="{Binding SelectedItem}" 
                    HorizontalAlignment="Left" Height="317" Margin="10,10,0,0" VerticalAlignment="Top" Width="659" Grid.ColumnSpan="2"></TabControl>

    </Grid>
</Window>
